const tokens = {
    client_id: process.env.CLIENT_ID,
    client_secret: process.env.CLIENT_SECRET,
};

const dataSources = {
    iexcloud: "https://sandbox.iexapis.com"
};

export {
    tokens,
    dataSources
}